---
export interface Props {
  title: string;
  href: string;
}

const { title, href = "#" } = Astro.props;
---

<a
  class="underline underline-offset-4 font-bold text-indigo-600 dark:text-amber-300 hover:opacity-90"
  href={href}
  target="_blank"
  rel="noopener noreferrer">{title}</a
>
